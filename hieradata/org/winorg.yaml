---
# passwords can be passed via org rather than the node
# lookup_options:
  #  "^profile::.*(psswrd|secretcontent|token)$":
  #  profile::win::psswrd: # '^profile::.+::sensitive_\w+$':
  #    convert_to: 'Sensitive' 
  #  profile::win::secretcontent:
    # convert_to: 'Sensitive'  
